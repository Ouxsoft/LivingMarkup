language: php

branches:
  only:
    - master

php:
  - 7.2
  - 7.3
  - 7.4

before_script:
  - composer update.
  - composer install

script:
  - mkdir -p build/logs
  - phpunit --coverage-clover build/logs/clover.xml

after_script:
  - bash <(curl -s https://codecov.io/bash)